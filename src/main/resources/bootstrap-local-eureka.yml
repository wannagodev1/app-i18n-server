spring:
  application.name: app-i18n-server
  cloud:
    config:
      uri: http://localhost:8888
      fail-fast: true
      username: user
      password: none
    refresh:
      enabled: false
  main:
    allow-bean-definition-overriding: true
